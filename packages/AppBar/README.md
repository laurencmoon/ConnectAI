# AppBar Component Package

This package contains the AppBar component for the top navigation bar.

## How to Use

1.  Copy this `AppBar` folder into your React project (e.g., into your `src/components/` directory).
2.  Import and use the `AppBar` component in your app.

## Dependencies
This component assumes you have `google-symbols` font available in your project.
